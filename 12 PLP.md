วันที่ 28/11/2552
- อยากลองทำอะไรใหม่ๆ และเร่งหาสิ่งที่ตัวเองชอบ เพื่อที่จะนำไปต่อยอดในอนาคตสามารถนำไปเป็นอาชีพได้
- เขียนโปรแกรมเวลาแจ้งเตือนสิ่งที่ต้องทำก่อนที่จะเล่นโทรศัพท์เช่น การตื่นนอน กินข้าว อาบน้ำ


โค๊ด
import tkinter as tk
import webbrowser

# ฟังก์ชันเปิดลิงก์
def open_link(url):
    webbrowser.open(url)

# ฟังก์ชันแสดงผล
def show_result():
    thing = entry.get().strip()

    if thing == "ออกกำลังกาย":
        result_text.set("ช่วงเช้า 06:00 - 07:00 น.\nเป็นช่วงเวลาที่ดีในการเผาผลาญไขมันและลดน้ำหนักได้ดี")
        link.set("https://www.technogym.com/my/newsroom/when-train-muscle-mass/")

    elif thing == "อ่านหนังสือ":
        result_text.set("ช่วงเย็น 16:00 - 19:00 น. หรือถึง 21:00 น.\nสมองทำงานได้อย่างมีประสิทธิภาพ")
        link.set("https://news.msu.ac.th/msumagaz/smain/readpost.php?mid=510")

    elif thing == "นอน":
        result_text.set("ควรนอนให้ครบ 7-9 ชั่วโมง\nเพื่อสุขภาพและการซ่อมแซมร่างกาย")
        link.set("https://www.dibukhospital.com/")

    elif thing == "ทำการบ้าน":
        result_text.set("ช่วง 16:00 - 18:00 น.\nสมองยังตื่นตัวและจำเนื้อหาได้ดี")
        link.set("https://www.goldencup.co.th/All-article/Mother-and-Baby/Managing-Homework-Time-A-Key-Skill-Children-Should-Learn")

    else:
        result_text.set("ยังไม่มีข้อมูลเวลาที่เหมาะสมสำหรับสิ่งนี้")
        link.set("")

# สร้างหน้าต่าง
window = tk.Tk()
window.title("🌿 โปรแกรมแจ้งเตือนสิ่งที่ต้องทำ")
window.geometry("500x350")
window.configure(bg="#d0f0c0")  # สีเขียวอ่อน

# ตัวแปรข้อความ
result_text = tk.StringVar()
link = tk.StringVar()

# หัวข้อ
title_label = tk.Label(
    window,
    text="🌿 โปรแกรมแจ้งเตือนสิ่งที่ต้องทำ 🌿",
    font=("Arial", 16, "bold"),
    bg="#2e8b57",
    fg="white",
    pady=10
)
title_label.pack(fill="x")

# ช่องกรอกข้อมูล
entry_label = tk.Label(window, text="กรอกสิ่งที่ต้องทำ:", bg="#d0f0c0", font=("Arial", 12))
entry_label.pack(pady=10)

entry = tk.Entry(window, width=30, font=("Arial", 12))
entry.pack(pady=5)

# ปุ่ม
button = tk.Button(
    window,
    text="แสดงเวลาที่เหมาะสม",
    command=show_result,
    bg="#3cb371",
    fg="white",
    font=("Arial", 12, "bold")
)
button.pack(pady=15)

# แสดงผลลัพธ์
result_label = tk.Label(
    window,
    textvariable=result_text,
    bg="#d0f0c0",
    fg="#004d00",
    font=("Arial", 12),
    wraplength=450,
    justify="center"
)
result_label.pack(pady=10)

# ลิงก์อ้างอิง (คลิกได้)
link_label = tk.Label(
    window,
    textvariable=link,
    fg="blue",
    cursor="hand2",
    bg="#d0f0c0",
    font=("Arial", 10, "underline")
)
link_label.pack()

link_label.bind("<Button-1>", lambda e: open_link(link.get()))

# เริ่มโปรแกรม
window.mainloop()

